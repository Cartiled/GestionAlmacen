# GestionAlmacen
Un sistema de gestión de almacenes simple con separación de frontend y backend utiliza Spring Boot para el backend y Vue para el frontend, implementando que el backend cree interfaces REST API mientras que el frontend, mediante Axios, realiza llamadas entre dominios (CORS) a las interfaces para llevar a cabo una serie de operaciones de usuario.
